# Intake brief — P2, K-ADV-PE
# Topic: P2 · Backstage Deep Dive
# Module P2 · Backstage

lesson:
  title: 'P2 · Backstage Deep Dive'
  slug: 'p2-backstage'
  domain: 'kubernetes'
  course_slug: 'advanced-platform-engineering'
  position: 02
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Workshop · Catalog Drawer — The Catalog Drawer in K-Workshop holds index cards for every tool, every blueprint, every artifact. The card lists who built it, who owns it, what it does, where its docs live (TechDocs cabinet), how '
  module: 'Module P2 · Backstage'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-PE modules 1-1 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain every Component / Service / API / Resource / System registered.'
  - 'Learner can explain per-repo MkDocs rendered centrally.'
  - 'Learner can explain template engine generating repos + manifests.'

sections:
  - 'Every Component / Service / API / Resource / System registered'
  - 'Per-repo MkDocs rendered centrally'
  - 'Template engine generating repos + manifests'
  - 'Integrations + extensibility'

section_5_real_world:
  count: 4
  examples:
    - 'On-call sees a failing Service; Backstage Catalog → owner + Slack channel + on-call rotation in 10 seconds.'
    - '200-repo org enables Backstage GitHub integration; existing repos with catalog-info.'
    - '"New Go service" template ships repo + Helm + CI + observability + Argo CD app.'
    - 'Migration deleted services; catalog still listed them.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-pe-lesson-02.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-pe-lesson-02.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Backstage four pillars: Catalog, TechDocs, Scaffolder, Plugins. Catalog is the source of truth (every service registered with owner + tier). TechDocs surfaces every README; Scaffolder ships golden paths; Plugins integrate everything.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/platform-engineering'
  - 'https://kubernetes.io/docs/'
