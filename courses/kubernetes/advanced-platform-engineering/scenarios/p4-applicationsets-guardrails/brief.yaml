# Intake brief — P4, K-ADV-PE
# Topic: P4 · Argo CD ApplicationSets + OPA / Kyverno Guardrails
# Module P4 · ApplicationSets + Guardrails

lesson:
  title: 'P4 · Argo CD ApplicationSets + OPA / Kyverno Guardrails'
  slug: 'p4-applicationsets-guardrails'
  domain: 'kubernetes'
  course_slug: 'advanced-platform-engineering'
  position: 04
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Workshop · Batch-Crafting Jig — The Batch-Crafting Jig in the workshop fixtures one master template + many empty wagon frames; the master pours per-frame variations from a labels list. Three quality inspectors review at three checkp'
  module: 'Module P4 · ApplicationSets + Guardrails'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-PE modules 1-3 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain five generator kinds for fleet patterns.'
  - 'Learner can explain cluster labels feed template parameters.'
  - 'Learner can explain pR-time + admission + sync.'

sections:
  - 'Five generator kinds for fleet patterns'
  - 'Cluster labels feed template parameters'
  - 'PR-time + admission + sync'
  - 'staged rollouts + sync waves + ProgressiveSync'

section_5_real_world:
  count: 4
  examples:
    - 'Platform team''s ApplicationSet uses Cluster generator + values from cluster labels (tier/region/env).'
    - 'A tenant PR''d a privileged Pod.'
    - 'ProgressiveSync waved to dev → staging; staging health failed; rollout aborted before prod.'
    - 'ApplicationSet deployed; Pods image-pull-error fleet-wide.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-pe-lesson-04.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-pe-lesson-04.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**ApplicationSet generates per-cluster Apps from one template; OPA + Kyverno gate at PR + admission + sync. Fleet velocity + fleet safety.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/platform-engineering'
  - 'https://kubernetes.io/docs/'
