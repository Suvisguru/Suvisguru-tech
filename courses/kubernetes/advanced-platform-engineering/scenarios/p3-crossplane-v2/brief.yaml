# Intake brief — P3, K-ADV-PE
# Topic: P3 · Crossplane v2
# Module P3 · Crossplane v2

lesson:
  title: 'P3 · Crossplane v2'
  slug: 'p3-crossplane-v2'
  domain: 'kubernetes'
  course_slug: 'advanced-platform-engineering'
  position: 03
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Workshop · Composition Workbench — The Composition Workbench is where the Master Craftsperson designs reusable assemblies. The blueprint (XRD) declares what the customer asks for. The assembly instructions (Composition + Functions) exp'
  module: 'Module P3 · Crossplane v2'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-PE modules 1-2 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain k8s control plane for cloud resources.'
  - 'Learner can explain define the user API + render logic.'
  - 'Learner can explain pluggable composition logic — KCL / Pkl / Go / patch-and-transform.'

sections:
  - 'K8s control plane for cloud resources'
  - 'Define the user API + render logic'
  - 'Pluggable composition logic — KCL / Pkl / Go / patch-and-transform'
  - 'Provider ecosystem + shareable bundles'

section_5_real_world:
  count: 4
  examples:
    - 'Platform ships XPostgresInstance XRD + 3 Compositions (AWS RDS / Azure Database / GCP CloudSQL).'
    - 'XTenantNamespace XRD ships namespace + RBAC + NetPol + Quota + service-catalog entry.'
    - 'Platform team ships one ConfigurationPackage (XPostgres + XTenantNamespace + XBucket); 8 BU clusters install; same shape everywhere.'
    - 'A human edited an RDS instance via console; Crossplane saw drift; reconciled back to spec.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-pe-lesson-03.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-pe-lesson-03.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Crossplane v2: XRD = user-facing CRD; Composition + Functions = render logic; Providers = AWS / Azure / GCP / SaaS controllers; ConfigurationPackage = shareable bundle. Platform-as-API; one consistent shape for every tenant.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/platform-engineering'
  - 'https://kubernetes.io/docs/'
