# Intake brief — G7, K-GKE
# Topic: G7 · GKE Observability (Cloud Logging + Monitoring + GMP + Grafana + Trace + Profiler + SLO)
# Module G7 · GKE Observability

lesson:
  title: 'G7 · GKE Observability (Cloud Logging + Monitoring + GMP + Grafana + Trace + Profiler + SLO)'
  slug: 'g7-gke-observability'
  domain: 'kubernetes'
  course_slug: 'google-gke'
  position: 07
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Garden · Watchtower — The **Watchtower** at K-Garden is staffed all hours by three observers + an SLO scribe.
    The **Heartbeat Drummer** (metrics) plays a regular cadence: "how full are the plots? how warm is the climat'
  module: 'Module G7 · GKE Observability'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'GCP basics: IAM, VPC, GCE, Cloud LB, PD/Filestore, Cloud Logging/Monitoring.'
    - 'K-GKE modules 1-6 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain cloud Logging + Cloud Monitoring — auto-enabled for GKE.'
  - 'Learner can explain managed Service for Prometheus (GMP) + managed Grafana.'
  - 'Learner can explain cloud Trace + Cloud Profiler.'

sections:
  - 'Cloud Logging + Cloud Monitoring — auto-enabled for GKE'
  - 'Managed Service for Prometheus (GMP) + managed Grafana'
  - 'Cloud Trace + Cloud Profiler'
  - 'Control-plane metrics/logs + SLO Monitoring + alerting policies'

section_5_real_world:
  count: 4
  examples:
    - 'A SaaS team builds a per-service RED dashboard (Rate, Errors, Duration) in managed Grafana.'
    - 'Bank enables --monitoring=APISERVER + audit log routing.'
    - 'Team had threshold alerts: "alert if 5xx rate > 1%.'
    - 'Cloud Profiler flame graphs review: a recent code change introduced a regex compile in a hot loop, burning 30% of pod CPU.'

section_6_animated_svg:
  modes_count: 1-2
  description: 'See preview-kubernetes-gke-lesson-07.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-gke-lesson-07.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Three pipes auto-enabled (Cloud Monitoring metrics + Cloud Logging logs + Cloud Trace spans). GMP for Prometheus + managed Grafana for the joined view. SLO-first alerting via Service Monitoring. BigQuery cost export ties everything to spend.**'

fact_check_anchors:
  - 'https://cloud.google.com/kubernetes-engine/docs/'
  - 'https://cloud.google.com/kubernetes-engine/docs/best-practices'
  - 'https://kubernetes.io/docs/'
