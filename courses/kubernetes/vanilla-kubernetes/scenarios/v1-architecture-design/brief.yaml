# Intake brief — V1, K-VAN
# Topic: V1 · Production Architecture Design for Self-Managed K8s
# Module V1 · Production Architecture

lesson:
  title: 'V1 · Production Architecture Design for Self-Managed K8s'
  slug: 'v1-architecture-design'
  domain: 'kubernetes'
  course_slug: 'vanilla-kubernetes'
  position: 01
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Frontier · Drafting Hut — The Drafting Hut is the first stop on the K-Frontier homestead. Before a single tool comes out, you sit at the survey table and draw what you''re building: where the well goes, where the main house sit'
  module: 'Module V1 · Production Architecture'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'K-VAN modules 1-0 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain why the architecture phase exists.'
  - 'Learner can explain three sizing axes.'
  - 'Learner can explain cIDRs, DNS, and the LB.'

sections:
  - 'Why the architecture phase exists'
  - 'Three sizing axes'
  - 'CIDRs, DNS, and the LB'
  - 'OS, runtime, CNI, CSI, ingress, backup'

section_5_real_world:
  count: 4
  examples:
    - '3 control-plane + 5 worker bare-metal cluster.'
    - '5 dedicated etcd nodes (NVMe, isolated subnet) + 5 control-plane + 30 worker nodes across 3 racks.'
    - 'First cluster: 2 CP nodes (no quorum), shared etcd disks with kubelet, Pod CIDR `10.'
    - 'Management cluster runs Cluster API.'

section_6_animated_svg:
  modes_count: 1-3
  description: 'See preview-kubernetes-vanilla-lesson-01.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-vanilla-lesson-01.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - 'Production self-managed K8s starts as a **blueprint**: control-plane sizing, worker sizing, etcd sizing, API LB, Pod/Service CIDR, CNI, CSI, OS choice, runtime, HA topology (stacked vs external etcd), backup, upgrade, security baseline. Get the blueprint right; everything downstream is mechanical.'

fact_check_anchors:
  - 'https://kubernetes.io/docs/'
  - 'https://github.com/kubernetes/kubeadm'
