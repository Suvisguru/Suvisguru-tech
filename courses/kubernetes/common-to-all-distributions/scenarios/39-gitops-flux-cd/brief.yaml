# Intake brief — Lesson 39, K-COM
# Topic: GitOps with Flux CD · Multi-Controller Architecture
# Module 17 · GitOps · Lesson 2 of 3

lesson:
  title: 'GitOps with Flux CD · Multi-Controller Architecture'
  slug: '39-gitops-flux-cd'
  domain: 'kubernetes'
  course_slug: 'common-to-all-distributions'
  position: 39
  granularity: 'concept'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'Public Library — Flux CD Wing — The Public Library has a Flux wing alongside the Argo CD reading room. Same goal — keep shelves matching the catalogue — but different staffing model. Instead of one librarian doing everything, Flux has **specialised desks**: a fetching des'
  module: 'Module 17 · GitOps · Lesson 2 of 3'

learner_profile:
  prerequisites:
    - 'Lessons 1-38 of K-COM (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain same goal, different architecture.'
  - 'Learner can explain "Flux bootstrapping its own GitOps".'
  - 'Learner can explain gitRepository, Kustomization, HelmRelease.'

sections:
  - 'Same goal, different architecture'
  - '"Flux bootstrapping its own GitOps"'
  - 'GitRepository, Kustomization, HelmRelease'
  - 'The pieces beyond sync'

section_5_real_world:
  count: 4
  examples:
    - 'CI builds + pushes signed images.'
    - 'Each tenant gets a namespace + a Flux Kustomization scoped to their git path.'
    - 'cert-manager, Argo Rollouts, Prometheus, etc.'
    - 'Engineering team mostly senior with deep K8s knowledge.'

section_6_animated_svg:
  modes_count: 3
  description: 'See preview-kubernetes-lesson-39.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-lesson-39.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - 'Flux uses **specialised controllers per concern**: `GitRepository` / `HelmRepository` / `OCIRepository` for sources; `Kustomization` for Kustomize-based deploys; `HelmRelease` for Helm; `Alert` + `Provider` for notifications. CLI is `flux`. CRD-only — no built-in UI. Best for: GitOps-native shops that prefer kubectl as the interface.'

fact_check_anchors:
  - 'https://kubernetes.io/docs/'
