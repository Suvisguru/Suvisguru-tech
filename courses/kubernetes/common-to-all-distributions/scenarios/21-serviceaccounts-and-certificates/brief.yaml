# Intake brief — Lesson 21, K-COM
# Topic: ServiceAccounts & Certificates · Tokens, Projected JWTs, cert-manager, Cluster PKI
# Module 10 · Config & Identity · Lesson 2 of 2

lesson:
  title: 'ServiceAccounts & Certificates · Tokens, Projected JWTs, cert-manager, Cluster PKI'
  slug: '21-serviceaccounts-and-certificates'
  domain: 'kubernetes'
  course_slug: 'common-to-all-distributions'
  position: 21
  granularity: 'concept'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'Permit Office — Identity Bureau — The Permit Office issues two kinds of paperwork. The **passport** (ServiceAccount) is the identity itself: it says "this is app-x, vouched for by the city." The **travel pass** (token) is what app-x actually presents at every checkpoint: it'
  module: 'Module 10 · Config & Identity · Lesson 2 of 2'

learner_profile:
  prerequisites:
    - 'Lessons 1-20 of K-COM (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain workload identity vs human identity.'
  - 'Learner can explain projected vs legacy ServiceAccount tokens.'
  - 'Learner can explain why every K8s component has a certificate.'

sections:
  - 'Workload identity vs human identity'
  - 'Projected vs legacy ServiceAccount tokens'
  - 'Why every K8s component has a certificate'
  - 'Three CRDs you actually use'

section_5_real_world:
  count: 4
  examples:
    - 'The Pod''s projected JWT has audience `sts.'
    - 'cert-manager has a Vault PKI ClusterIssuer.'
    - 'Install cert-manager → apply a `ClusterIssuer` for Let''s Encrypt → add `cert-manager.'
    - 'Found 47 ServiceAccount Secrets created pre-1.'

section_6_animated_svg:
  modes_count: 3
  description: 'See preview-kubernetes-lesson-21.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-lesson-21.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - 'Every Pod has a **ServiceAccount** (its workload identity). Modern clusters use **projected, short-lived, audience-bound JWTs** — *not* the legacy long-lived Secret tokens. For TLS certs (Ingress, webhooks, mTLS), use **cert-manager** — never hand-roll OpenSSL.'

fact_check_anchors:
  - 'https://kubernetes.io/docs/'
