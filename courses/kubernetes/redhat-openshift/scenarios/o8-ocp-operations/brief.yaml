# Intake brief — O8, K-OCP
# Topic: O8 · OpenShift Operations
# Module O8 · OpenShift Operations

lesson:
  title: 'O8 · OpenShift Operations'
  slug: 'o8-ocp-operations'
  domain: 'kubernetes'
  course_slug: 'redhat-openshift'
  position: 08
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Foundry · Maintenance Bay — The **Maintenance Bay** is where the foundry stays running. Three crews operate.
    The **Version Crew** (CVO + ClusterOperators) tracks the foundry''s declared version + ensures all 30+ specialty ope'
  module: 'Module O8 · OpenShift Operations'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'Reference version: OpenShift Container Platform 4.21+.'
    - 'K-OCP modules 1-7 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain clusterVersion + update channels + EUS.'
  - 'Learner can explain machineConfig + MachineConfigPools + MachineSets + MachineHealthChecks.'
  - 'Learner can explain etcd backup + must-gather + Insights telemetry + support cases.'

sections:
  - 'ClusterVersion + update channels + EUS'
  - 'MachineConfig + MachineConfigPools + MachineSets + MachineHealthChecks'
  - 'etcd backup + must-gather + Insights telemetry + support cases'
  - 'Disconnected updates + upgrade risk assessment'

section_5_real_world:
  count: 4
  examples:
    - 'A regulated bank''s payments cluster runs OCP on the EUS channel.'
    - 'Telco running 5G UPF needs CPU pinning + hugepages + RT kernel.'
    - 'Air-gapped SCIF facility.'
    - 'A bank tests etcd disaster recovery quarterly.'

section_6_animated_svg:
  modes_count: 1-2
  description: 'See preview-kubernetes-ocp-lesson-08.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-ocp-lesson-08.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**CVO orchestrates ClusterOperators on a channel; pick stable for prod, fast for canary, EUS for regulated. MCO + MachineConfigPools roll node configs sequentially with PDB-aware drain. Always wire etcd backup + mirror registry + must-gather before you need them.**'

fact_check_anchors:
  - 'https://docs.openshift.com/'
  - 'https://access.redhat.com/documentation/en-us/openshift_container_platform/'
  - 'https://kubernetes.io/docs/'
