# Intake brief — O3, K-OCP
# Topic: O3 · OpenShift Networking
# Module O3 · OpenShift Networking

lesson:
  title: 'O3 · OpenShift Networking'
  slug: 'o3-ocp-networking'
  domain: 'kubernetes'
  course_slug: 'redhat-openshift'
  position: 03
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Foundry · Pipework &amp; Conveyors — The **Pipework & Conveyors** are the foundry''s hidden infrastructure. The **OVN-Kubernetes Conveyor System** is the standardized rail network — every cart (Pod) gets an address (IP from cluster networ'
  module: 'Module O3 · OpenShift Networking'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'Reference version: OpenShift Container Platform 4.21+.'
    - 'K-OCP modules 1-2 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain oVN-Kubernetes + cluster/service/machine networks.'
  - 'Learner can explain routes + Ingress + Gateway API — three ingress paths.'
  - 'Learner can explain networkPolicy + EgressIP + Egress firewall + Egress router.'

sections:
  - 'OVN-Kubernetes + cluster/service/machine networks'
  - 'Routes + Ingress + Gateway API — three ingress paths'
  - 'NetworkPolicy + EgressIP + Egress firewall + Egress router'
  - 'Multus + SR-IOV / DPDK + MetalLB + NetObserv + Submariner'

section_5_real_world:
  count: 4
  examples:
    - 'A bank''s payment Project must call an external partner API that whitelists by source IP.'
    - 'Telco running 5G User Plane Function on OCP.'
    - 'Bare-metal OCP cluster needs `type: LoadBalancer` Services.'
    - 'Active-active OCP deployment in two regions.'

section_6_animated_svg:
  modes_count: 1-2
  description: 'See preview-kubernetes-ocp-lesson-03.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-ocp-lesson-03.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**OVN-Kubernetes is default; SDN is removed. Routes for TLS-aware ingress; Gateway API for modern multi-cluster. NetworkPolicy + EgressIP + Egress firewall for control. Multus + SR-IOV for telco. MetalLB for bare-metal LoadBalancer. NetObserv for flow visibility.**'

fact_check_anchors:
  - 'https://docs.openshift.com/'
  - 'https://access.redhat.com/documentation/en-us/openshift_container_platform/'
  - 'https://kubernetes.io/docs/'
