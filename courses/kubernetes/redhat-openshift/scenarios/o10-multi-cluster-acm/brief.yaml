# Intake brief — O10, K-OCP
# Topic: O10 · Multi-Cluster with ACM (RHACM)
# Module O10 · Multi-Cluster with ACM

lesson:
  title: 'O10 · Multi-Cluster with ACM (RHACM)'
  slug: 'o10-multi-cluster-acm'
  domain: 'kubernetes'
  course_slug: 'redhat-openshift'
  position: 10
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Foundry · Multi-Foundry Network — The **Multi-Foundry Network** is K-Foundry''s federation hub. RHACM''s hub cluster sits in the centre; spoke foundries register as **ManagedClusters**. Each spoke runs a **klusterlet agent** that phones'
  module: 'Module O10 · Multi-Cluster with ACM'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'Reference version: OpenShift Container Platform 4.21+.'
    - 'K-OCP modules 1-9 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain managedCluster + Placement + cluster lifecycle (HCP).'
  - 'Learner can explain applicationSet (Argo CD-based) + Subscription model.'
  - 'Learner can explain policy + PlacementBinding — fleet-wide governance.'

sections:
  - 'ManagedCluster + Placement + cluster lifecycle (HCP)'
  - 'ApplicationSet (Argo CD-based) + Subscription model'
  - 'Policy + PlacementBinding — fleet-wide governance'
  - 'ObservabilityAddon + Hosted Control Planes at scale + Submariner'

section_5_real_world:
  count: 4
  examples:
    - 'A bank registers 60 clusters into RHACM hub.'
    - 'Telco runs OCP at 800 cell sites.'
    - 'A SaaS gives each enterprise tenant their own OCP cluster.'
    - 'OCP clusters in 3 regions registered to RHACM hub.'

section_6_animated_svg:
  modes_count: 1-2
  description: 'See preview-kubernetes-ocp-lesson-10.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-ocp-lesson-10.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**RHACM hub manages many OCP + non-OCP K8s clusters as ManagedClusters. Placement selects by labels; ApplicationSet pushes apps; Policy + PlacementBinding governs fleet-wide; ObservabilityAddon aggregates. Add HCP for cluster-density + Submariner for cross-cluster networking.**'

fact_check_anchors:
  - 'https://docs.openshift.com/'
  - 'https://access.redhat.com/documentation/en-us/openshift_container_platform/'
  - 'https://kubernetes.io/docs/'
