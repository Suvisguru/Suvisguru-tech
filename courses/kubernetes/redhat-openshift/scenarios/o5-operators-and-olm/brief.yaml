# Intake brief — O5, K-OCP
# Topic: O5 · Operators and OLM
# Module O5 · Operators and OLM

lesson:
  title: 'O5 · Operators and OLM'
  slug: 'o5-operators-and-olm'
  domain: 'kubernetes'
  course_slug: 'redhat-openshift'
  position: 05
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Foundry · Operator Hub — The **Operator Hub** at K-Foundry is where you order specialty machinery. Four catalogs sit on the counter: Red Hat''s officially-supported parts, Certified third-party parts (IBM, NetApp, etc.), Commu'
  module: 'Module O5 · Operators and OLM'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'Reference version: OpenShift Container Platform 4.21+.'
    - 'K-OCP modules 1-4 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain operator Hub + CatalogSources.'
  - 'Learner can explain subscription + InstallPlan + CSV — the install pipeline.'
  - 'Learner can explain operatorGroup + scope (AllNamespaces / SingleNamespace / OwnNamespace).'

sections:
  - 'Operator Hub + CatalogSources'
  - 'Subscription + InstallPlan + CSV — the install pipeline'
  - 'OperatorGroup + scope (AllNamespaces / SingleNamespace / OwnNamespace)'
  - 'Dependencies + broken-operator recovery + custom / certified operators'

section_5_real_world:
  count: 4
  examples:
    - 'A regulated bank uses only operators from their internal CatalogSource (mirrored from Red Hat + curated by the platform team).'
    - 'A SaaS where each tenant gets their own Postgres database.'
    - 'Team installs cert-manager Operator.'
    - 'An operator CSV stuck in Failed for 3 days due to CRD-conflict drama.'

section_6_animated_svg:
  modes_count: 1-2
  description: 'See preview-kubernetes-ocp-lesson-05.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-ocp-lesson-05.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**OLM lifecycle: CatalogSource → Subscription (channel + approval) → InstallPlan (deps + RBAC) → CSV (deploys operator). OperatorGroup defines scope. Pick channel by stability tolerance; pick approval mode by upgrade discipline; resolve dependency conflicts before installing.**'

fact_check_anchors:
  - 'https://docs.openshift.com/'
  - 'https://access.redhat.com/documentation/en-us/openshift_container_platform/'
  - 'https://kubernetes.io/docs/'
