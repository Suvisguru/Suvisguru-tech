# Intake brief — O12, K-OCP
# Topic: O12 · OpenShift Troubleshooting
# Module O12 · OpenShift Troubleshooting

lesson:
  title: 'O12 · OpenShift Troubleshooting'
  slug: 'o12-ocp-troubleshooting'
  domain: 'kubernetes'
  course_slug: 'redhat-openshift'
  position: 12
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Foundry · Diagnostic Lab — The **Diagnostic Lab** at K-Foundry is where you go when something''s wrong. The triage nurse asks the same questions every time, in the same order.
    First: *"What does the master operator board (CV'
  module: 'Module O12 · OpenShift Troubleshooting'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'Reference version: OpenShift Container Platform 4.21+.'
    - 'K-OCP modules 1-11 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain clusterOperator + CVO + MachineConfigPool triage.'
  - 'Learner can explain sCC denial + Routes + cert + registry + OAuth failures.'
  - 'Learner can explain build + CSV + CatalogSource + OVN-K + DNS Operator failures.'

sections:
  - 'ClusterOperator + CVO + MachineConfigPool triage'
  - 'SCC denial + Routes + cert + registry + OAuth failures'
  - 'Build + CSV + CatalogSource + OVN-K + DNS Operator failures'
  - 'etcd recovery + disconnected pull failures + the diagnostic toolkit'

section_5_real_world:
  count: 4
  examples:
    - 'DNS Operator goes degraded.'
    - 'MCP/worker stuck Updating for 90 min.'
    - 'Helm chart fails admission: "unable to validate against any SCC.'
    - 'A bank simulates etcd quorum loss on a dev cluster.'

section_6_animated_svg:
  modes_count: 1-2
  description: 'See preview-kubernetes-ocp-lesson-12.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-ocp-lesson-12.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**OCP triage playbook: ClusterOperator status first → CVO state → MachineConfigPool roll status → recent Activity. Diagnostic toolkit: must-gather (default + targeted) + oc adm inspect + oc debug node. Eight common failure pattern families with documented recovery paths.**'

fact_check_anchors:
  - 'https://docs.openshift.com/'
  - 'https://access.redhat.com/documentation/en-us/openshift_container_platform/'
  - 'https://kubernetes.io/docs/'
