# Intake brief — O1, K-OCP
# Topic: O1 · OpenShift Architecture
# Module O1 · OpenShift Architecture

lesson:
  title: 'O1 · OpenShift Architecture'
  slug: 'o1-ocp-architecture'
  domain: 'kubernetes'
  course_slug: 'redhat-openshift'
  position: 01
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Foundry · Welcome Hall — K-Foundry is the Red Hat enterprise factory. The **Welcome Hall** is where every visitor enters. On the wall is a giant floor plan showing the whole foundry: 13 bays, each handling a different product'
  module: 'Module O1 · OpenShift Architecture'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'Reference version: OpenShift Container Platform 4.21+.'
    - 'K-OCP modules 1-0 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain what OCP adds on top of upstream K8s.'
  - 'Learner can explain eight deployment shapes — pick the operating model.'
  - 'Learner can explain rHCOS + MCO + CVO + OLM — the four operators that run OCP.'

sections:
  - 'What OCP adds on top of upstream K8s'
  - 'Eight deployment shapes — pick the operating model'
  - 'RHCOS + MCO + CVO + OLM — the four operators that run OCP'
  - 'Web console (Administrator + Developer) + edge variants'

section_5_real_world:
  count: 4
  examples:
    - 'A regulated bank standardising on OCP.'
    - 'A telco runs OCP at 800 cell sites.'
    - 'A platform team running 6 self-managed K8s clusters with Argo CD, Prometheus, Loki, Linkerd, cert-manager, sealed-secrets, etc.'
    - 'A 100-engineer team.'

section_6_animated_svg:
  modes_count: 1-2
  description: 'See preview-kubernetes-ocp-lesson-01.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-ocp-lesson-01.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**OCP = upstream K8s + Red Hat''s opinionated additions (Routes, SCCs, OperatorHub-everywhere, integrated OAuth/registry/console). RHCOS is immutable + MCO-managed. CVO orchestrates ~30 cluster operators. Pick the deployment shape that matches your operating model: OCP / OSD / ROSA / ARO / OKD / HyperShift / MicroShift / CRC.**'

fact_check_anchors:
  - 'https://docs.openshift.com/'
  - 'https://access.redhat.com/documentation/en-us/openshift_container_platform/'
  - 'https://kubernetes.io/docs/'
