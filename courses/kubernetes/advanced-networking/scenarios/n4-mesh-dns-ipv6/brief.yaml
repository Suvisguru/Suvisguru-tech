# Intake brief — N4, K-ADV-NET
# Topic: N4 · Service Mesh + DNS + IPv6
# Module N4 · Mesh + DNS + IPv6

lesson:
  title: 'N4 · Service Mesh + DNS + IPv6'
  slug: 'n4-mesh-dns-ipv6'
  domain: 'kubernetes'
  course_slug: 'advanced-networking'
  position: 04
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Highway · Express Lanes — Express Lanes are the cluster''s high-speed conveyors. Three concerns interlock. The **express-lane operator** (mesh) decides which lanes get TLS-sealed envelopes + retries + metrics. The **sign-system'
  module: 'Module N4 · Mesh + DNS + IPv6'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-NET modules 1-3 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain istio · Linkerd · Cilium Service Mesh · Kuma.'
  - 'Learner can explain coreDNS + NodeLocal DNSCache + ndots tuning.'
  - 'Learner can explain cluster + CNI + Service + Pod IPv6 enablement.'

sections:
  - 'Istio · Linkerd · Cilium Service Mesh · Kuma'
  - 'CoreDNS + NodeLocal DNSCache + ndots tuning'
  - 'cluster + CNI + Service + Pod IPv6 enablement'
  - 'how mesh + DNS + IPv6 interact'

section_5_real_world:
  count: 4
  examples:
    - 'A 30-engineer team needs mTLS + retries + metrics.'
    - '500-node cluster; CoreDNS Pods at 90% CPU; queries 50K rps.'
    - 'A team auditing 200 services found 35 with hardcoded IPv4 binds.'
    - 'A 1000-Pod Istio sidecar deployment migrated to Istio ambient mode.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-net-lesson-04.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-net-lesson-04.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Pick mesh by needs (Istio rich; Linkerd simple-fast; Cilium eBPF-native). NodeLocal DNSCache + ndots:1 are non-optional at scale. IPv6 / dual-stack: enable per layer; legacy apps need readiness checks.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/kubernetes-networking'
  - 'https://kubernetes.io/docs/'
