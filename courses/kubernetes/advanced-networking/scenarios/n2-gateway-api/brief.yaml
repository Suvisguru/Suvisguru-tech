# Intake brief — N2, K-ADV-NET
# Topic: N2 · Gateway API at Fleet Scale
# Module N2 · Gateway API

lesson:
  title: 'N2 · Gateway API at Fleet Scale'
  slug: 'n2-gateway-api'
  domain: 'kubernetes'
  course_slug: 'advanced-networking'
  position: 02
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Highway · Main Intersection — The Main Intersection in K-Highway has three layers of authority. The **city operator** (GatewayClass) builds + maintains the intersection itself; chooses asphalt + signals + traffic-engineering vendo'
  module: 'Module N2 · Gateway API'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-NET modules 1-1 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain gatewayClass · Gateway · HTTPRoute.'
  - 'Learner can explain routes in one namespace pointing at backends in another.'
  - 'Learner can explain re-encrypt to backend + header/URL rewrites.'

sections:
  - 'GatewayClass · Gateway · HTTPRoute'
  - 'Routes in one namespace pointing at backends in another'
  - 're-encrypt to backend + header/URL rewrites'
  - 'fleet-wide ingress + the path from Ingress'

section_5_real_world:
  count: 4
  examples:
    - 'A 60-engineer org deploys one Gateway in `infra` namespace handling api.'
    - 'A team had 80 Ingress objects across 30 namespaces.'
    - 'A team needs api.'
    - 'On the legacy Ingress, a single typo in an NGINX annotation broke routing cluster-wide.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-net-lesson-02.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-net-lesson-02.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Gateway API: GatewayClass (infra) + Gateway (shared listener bank) + HTTPRoute (per-team). ReferenceGrant for cross-namespace; BackendTLSPolicy for re-encrypt. Replaces Ingress + per-controller annotations.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/kubernetes-networking'
  - 'https://kubernetes.io/docs/'
