# Intake brief — I7, K-ADV-AI
# Topic: I7 · GPU Sharing + Multi-Tenant Security + Cost Optimization
# Module I7 · GPU Sharing + Tenant + Cost

lesson:
  title: 'I7 · GPU Sharing + Multi-Tenant Security + Cost Optimization'
  slug: 'i7-gpu-sharing-tenant-cost'
  domain: 'kubernetes'
  course_slug: 'advanced-ai-ml-gpus'
  position: 07
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Observatory · Sharing Committee — The Sharing Committee at the observatory governs telescope use. Telescopes are split into eyepieces (MIG); some shared by appointment (time-slicing); booking goes through committee (DRA / Kueue). Diff'
  module: 'Module I7 · GPU Sharing + Tenant + Cost'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-AI modules 1-6 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain mIG vs time-slicing vs DRA vs MPS.'
  - 'Learner can explain isolation primitives + tenant boundary.'
  - 'Learner can explain spot + idle reclaim + Kueue queue + RIs + ARM.'

sections:
  - 'MIG vs time-slicing vs DRA vs MPS'
  - 'Isolation primitives + tenant boundary'
  - 'Spot + idle reclaim + Kueue queue + RIs + ARM'
  - 'Quotas + dashboards + chargeback'

section_5_real_world:
  count: 4
  examples:
    - 'Inference fleet on whole H100s; each Pod 20% util.'
    - 'Fault-tolerant training adopted Spot G5 + Spot G6 fleet; checkpoint every 15 min; reclaim handled gracefully.'
    - 'Showback dashboards revealed top spenders.'
    - 'Time-slicing deployed; one Pod''s GPU OOM crashed all sibling Pods.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-ai-lesson-07.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-ai-lesson-07.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Share via MIG / time-slice / DRA. Isolate via namespace + Quota + MIG. Economise via Spot + idle reclaim + Kueue queue + RIs/SP/CUDs + right-size. The expensive GPU deserves utilization + governance + cost discipline.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/kubernetes-ai-ml'
  - 'https://kubernetes.io/docs/'
