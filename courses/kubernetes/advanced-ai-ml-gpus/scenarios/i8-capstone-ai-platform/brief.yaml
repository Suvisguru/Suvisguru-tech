# Intake brief — I8, K-ADV-AI
# Topic: I8 · Capstone — Production AI Inference Platform
# Module I8 · Capstone AI Platform

lesson:
  title: 'I8 · Capstone — Production AI Inference Platform'
  slug: 'i8-capstone-ai-platform'
  domain: 'kubernetes'
  course_slug: 'advanced-ai-ml-gpus'
  position: 08
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Observatory · Operating Observatory — The Operating Observatory is K-Observatory at scale. Every telescope (GPU) is multi-tenant via MIG eyepieces; every astronomer''s session is queue-managed (Kueue + Volcano); inference goes through the '
  module: 'Module I8 · Capstone AI Platform'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-AI modules 1-7 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain gPU + Kueue + Volcano + MIG.'
  - 'Learner can explain kServe + vLLM + Kubeflow + KubeRay.'
  - 'Learner can explain aI Gateway + RDMA / EFA + JuiceFS + OCI.'

sections:
  - 'GPU + Kueue + Volcano + MIG'
  - 'KServe + vLLM + Kubeflow + KubeRay'
  - 'AI Gateway + RDMA / EFA + JuiceFS + OCI'
  - 'Multi-tenant + Spot + chargeback + observability + runbooks'

section_5_real_world:
  count: 4
  examples:
    - 'LLM platform launches: vLLM behind KServe behind Envoy AI Gateway; H100 fleet with MIG for inference; Kueue admission; semantic cache; multi-provider failover.'
    - 'us-east-1 GPU capacity exhausted; MultiKueue routes new training jobs to eu-west-1 H100 pool; tenants see no service interruption.'
    - 'Quarterly review: top spenders right-sized via MIG; fault-tolerant training migrated to Spot; aggregate GPU bill dropped 35%.'
    - 'Simulated mass Spot reclaim; checkpointing + Kueue requeue + MultiKueue failover absorbed gracefully.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-ai-lesson-08.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 5
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-ai-lesson-08.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 5 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Operating Observatory: GPU + DRA + Kueue + Volcano + KServe + vLLM + AI Gateway + RDMA + JuiceFS + OCI artifacts + multi-tenant + Spot + chargeback. Every K-ADV-AI concept woven into one production AI inference platform.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/kubernetes-ai-ml'
  - 'https://kubernetes.io/docs/'
