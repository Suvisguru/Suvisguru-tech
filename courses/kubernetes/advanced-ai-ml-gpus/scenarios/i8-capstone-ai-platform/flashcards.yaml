# Flashcards — K-ADV-AI I (count: 5)

cards:
  - id: 1
    front: |
      Capstone's 8 layers?
    back: |
      **Capacity** (GPU + DRA + MIG), **scheduling** (Kueue + Volcano + MultiKueue), **ML stack** (Ray + Kubeflow + KServe + JobSet), **serving** (vLLM / Triton / NIM), **L7** (AI Gateway), **fabric** (RDMA / EFA / GDS), **storage** (JuiceFS / Alluxio + OCI), **governance** (multi-tenant + Spot + chargeback).

  - id: 2
    front: |
      Per-tenant isolation primitives?
    back: |
      Namespace + RBAC + NetPol + GPU Quota + MIG isolation + node taints + AI Gateway token budget.

  - id: 3
    front: |
      Operational rhythm in capstone?
    back: |
      Quarterly game days (Spot cascade + GPU OOM + AI Gateway throttle) + monthly cost review + quarterly SLO retro + tenant chargeback.

  - id: 4
    front: |
      Multi-region pattern?
    back: |
      MultiKueue federates training across regions; KServe per-region inference + global LB routing.

  - id: 5
    front: |
      LLM serving path in capstone?
    back: |
      Client → AI Gateway (auth + budget + safety + cache) → KServe InferenceService → vLLM / Triton / NIM → response stream.
