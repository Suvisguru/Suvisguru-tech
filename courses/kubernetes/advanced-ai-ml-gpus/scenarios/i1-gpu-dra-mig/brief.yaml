# Intake brief — I1, K-ADV-AI
# Topic: I1 · GPU Nodes + Device Plugin + GPU Operator + MIG + DRA
# Module I1 · GPU + DRA + MIG

lesson:
  title: 'I1 · GPU Nodes + Device Plugin + GPU Operator + MIG + DRA'
  slug: 'i1-gpu-dra-mig'
  domain: 'kubernetes'
  course_slug: 'advanced-ai-ml-gpus'
  position: 01
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Observatory · Optics Bay — The Optics Bay houses the observatory''s telescopes (GPUs). Old practice: each astronomer booked a whole telescope for the night even if they only needed a wide-angle lens. New practice: **MIG** partit'
  module: 'Module I1 · GPU + DRA + MIG'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-AI modules 1-0 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain driver + container runtime + node labels.'
  - 'Learner can explain baseline + full-stack.'
  - 'Learner can explain hardware partition H100 / A100 into shareable instances.'

sections:
  - 'Driver + container runtime + node labels'
  - 'Baseline + full-stack'
  - 'Hardware partition H100 / A100 into shareable instances'
  - 'K8s-native multi-vendor advanced scheduling'

section_5_real_world:
  count: 4
  examples:
    - 'Inference workload using one H100 per Pod (most Pods using < 20% GPU).'
    - 'Cluster has NVIDIA + AMD GPUs.'
    - 'Pre-Op: bare-metal driver install drift across nodes; CUDA version mismatches.'
    - 'Pre-Op cluster: H100 driver upgraded on some nodes; CUDA 12.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-ai-lesson-01.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-ai-lesson-01.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**NVIDIA device plugin = baseline; GPU Operator = full stack; MIG = hardware partition for sharing; DRA = K8s-native multi-vendor advanced scheduling. Pick stack by GPU model + sharing needs.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/kubernetes-ai-ml'
  - 'https://kubernetes.io/docs/'
