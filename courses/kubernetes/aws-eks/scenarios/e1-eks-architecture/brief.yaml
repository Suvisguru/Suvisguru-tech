# Intake brief — E1, K-EKS
# Topic: E1 · EKS Architecture and Shared Responsibility
# Module E1 · EKS Architecture

lesson:
  title: 'E1 · EKS Architecture and Shared Responsibility'
  slug: 'e1-eks-architecture'
  domain: 'kubernetes'
  course_slug: 'aws-eks'
  position: 01
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Skyline · Lobby & Floor Plan — Welcome to K-Skyline — the AWS Region''s premier managed-K8s tower. The lobby is bright; the floor plan is on the wall. The building manager (AWS) operates the elevators (apiserver), the central HVAC ('
  module: 'Module E1 · EKS Architecture'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'AWS basics: VPC, IAM, EC2, S3, CloudWatch.'
    - 'K-EKS modules 1-0 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain what EKS is and isn''t.'
  - 'Learner can explain where your Pods actually run.'
  - 'Learner can explain the five EKS-specific decisions you make once.'

sections:
  - 'What EKS is and isn''t'
  - 'Where your Pods actually run'
  - 'The five EKS-specific decisions you make once'
  - 'How clusters get created'

section_5_real_world:
  count: 4
  examples:
    - 'Single multi-AZ cluster.'
    - 'Cluster endpoint private only.'
    - 'Single YAML file per cluster (dev/staging/prod).'
    - 'Terraform module defines: VPC, EKS cluster, managed NG, Fargate profiles, IAM roles, IRSA roles, add-ons (CNI/CoreDNS/EBS CSI), Karpenter, ALB controller, ExternalDNS.'

section_6_animated_svg:
  modes_count: 1-3
  description: 'See preview-kubernetes-eks-lesson-01.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-eks-lesson-01.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - 'EKS = AWS runs control plane (apiserver, etcd, scheduler, controller-manager) across 3 AZs. You run the rest: nodes (5 options), VPC, IAM, workloads, cost. **Version policy**: 14 months standard support + 12 months extended support per minor. Track AWS''s EKS version lifecycle, not just upstream. Provision via Console / AWS CLI / eksctl / Terraform / EKS Blueprints / CDK / Pulumi / Crossplane.'

fact_check_anchors:
  - 'https://docs.aws.amazon.com/eks/latest/userguide/'
  - 'https://docs.aws.amazon.com/eks/latest/best-practices/'
  - 'https://kubernetes.io/docs/'
