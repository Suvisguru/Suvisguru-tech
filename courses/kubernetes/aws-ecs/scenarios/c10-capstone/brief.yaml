# Intake brief — C10, K-ECS (non-Kubernetes companion course)
# Topic: C10 · Capstone — The Reference Multi-Service Fargate Harbor
# Module C10 · Capstone

lesson:
  title: 'C10 · Capstone — The Reference Multi-Service Fargate Harbor'
  slug: 'c10-capstone'
  domain: 'kubernetes'
  course_slug: 'aws-ecs'
  position: 10
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Harbor · Grand Voyage — The harbor master arrives at the **Grand Voyage** ceremony. Every pier is wired correctly. The signal flag tower (ALB) routes incoming traffic; the smart relay station (Service Connect) handles east-w'
  module: 'Module C10 · Capstone'

course_disclaimer:
  non_kubernetes: true
  rationale: 'AWS ECS uses its own APIs (Cluster / Service / Task / Task Definition / Capacity Provider / Service Connect) — not K8s APIs. Included as a companion to K-EKS because organisations frequently choose between or coexist with EKS.'

learner_profile:
  prerequisites:
    - 'AWS basics: IAM, VPC, EC2, ALB, ECR, CloudWatch, Secrets Manager, KMS.'
    - 'Container fundamentals: Docker, image registries, container lifecycle.'
    - 'K-ECS modules 1-9 (cumulative).'
  assumed_zero_knowledge_of:
    - 'Kubernetes Pods / Deployments / Services / Ingress / RBAC / CRDs.'

learning_outcomes:
  - 'Learner can explain two Services, one ALB, Service Connect, EFS, Secrets.'
  - 'Learner can explain three roles per Service; least-privilege by default.'
  - 'Learner can explain blue/green + circuit breaker + autoscaling + telemetry.'

sections:
  - 'Two Services, one ALB, Service Connect, EFS, Secrets'
  - 'Three roles per Service; least-privilege by default'
  - 'Blue/green + circuit breaker + autoscaling + telemetry'
  - 'Every cause class → a 5-minute fix'

section_5_real_world:
  count: 4
  examples:
    - 'A 30-engineer SaaS replaces their single-EC2 + Compose stack with this reference.'
    - 'Tax-deadline traffic 8× normal.'
    - 'A Friday deploy raised P95 latency above the 800ms alarm threshold during the 10% canary phase.'
    - 'The team ran a quarterly game day on staging.'

section_6_animated_svg:
  modes_count: 1-2
  description: 'See preview-kubernetes-ecs-lesson-10.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-ecs-lesson-10.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**The reference K-Harbor: Service Connect + ALB blue/green + EFS + Secrets/KMS + Container Insights + FireLens + ADOT/X-Ray + Service Auto Scaling + Fargate/Spot mix + circuit breaker + runbook. Every concept in C1-C9 has a place; the runbook closes the loop.**'

fact_check_anchors:
  - 'https://docs.aws.amazon.com/ecs/'
  - 'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/Welcome.html'
  - 'https://docs.aws.amazon.com/AmazonECS/latest/bestpracticesguide/intro.html'
