# Intake brief — S2, K-ADV-SEC
# Topic: S2 · RBAC Design at Scale
# Module S2 · RBAC at Scale

lesson:
  title: 'S2 · RBAC Design at Scale'
  slug: 's2-rbac-at-scale'
  domain: 'kubernetes'
  course_slug: 'advanced-security'
  position: 02
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Citadel · Authorization Desk — The Authorization Desk is the second wall of the Citadel. Every visitor presents a citizenship paper (subject), the gate-keeper consults a ledger of **verbs** (Role) — "may enter the courtyard, may re'
  module: 'Module S2 · RBAC at Scale'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-SEC modules 1-1 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain subject, Role / ClusterRole, RoleBinding / ClusterRoleBinding.'
  - 'Learner can explain built-in admin / edit / view + custom aggregation.'
  - 'Learner can explain humans via OIDC, workloads via SA — different patterns.'

sections:
  - 'Subject, Role / ClusterRole, RoleBinding / ClusterRoleBinding'
  - 'Built-in admin / edit / view + custom aggregation'
  - 'Humans via OIDC, workloads via SA — different patterns'
  - 'audit2rbac, rakkess, kubectl-who-can, periodic narrowing'

section_5_real_world:
  count: 4
  examples:
    - 'A 200-engineer org uses Okta.'
    - 'An app reads ConfigMaps + lists Pods.'
    - 'Cluster-admin removed for all standing bindings.'
    - 'An auditor sampled SAs in a regulated cluster; 17/20 had cluster-admin.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-sec-lesson-02.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-sec-lesson-02.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**RBAC = subject + role + binding. Use ClusterRole-aggregation for composability. Map OIDC groups to Roles, never to cluster-admin. Run audit-driven narrowing (audit2rbac) quarterly. Default-deny is the cluster''s posture; cluster-admin is reserved for break-glass.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/kubernetes-security'
  - 'https://kubernetes.io/docs/'
