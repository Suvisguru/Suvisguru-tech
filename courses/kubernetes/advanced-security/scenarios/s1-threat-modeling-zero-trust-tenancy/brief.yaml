# Intake brief — S1, K-ADV-SEC
# Topic: S1 · Threat Modeling, Zero-Trust, Multi-Tenant Isolation
# Module S1 · Threat Modeling, Zero-Trust, Tenancy

lesson:
  title: 'S1 · Threat Modeling, Zero-Trust, Multi-Tenant Isolation'
  slug: 's1-threat-modeling-zero-trust-tenancy'
  domain: 'kubernetes'
  course_slug: 'advanced-security'
  position: 01
  granularity: 'module'
  brief_drafted_on: '2026-05-04'
  central_metaphor: 'K-Citadel · Threat Map Room — K-Citadel is a fortified citadel. The Threat Map Room is the first chamber every architect visits — pinned to the wall is a STRIDE wheel, a list of every entry point, every internal corridor, and ever'
  module: 'Module S1 · Threat Modeling, Zero-Trust, Tenancy'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'At least one distribution course (K-EKS / K-AKS / K-GKE / K-OCP / K-VAN).'
    - 'K-ADV-SEC modules 1-0 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain sTRIDE applied to a Kubernetes cluster.'
  - 'Learner can explain no implicit trust at any boundary.'
  - 'Learner can explain soft (namespace) vs hard (vCluster / per-cluster).'

sections:
  - 'STRIDE applied to a Kubernetes cluster'
  - 'No implicit trust at any boundary'
  - 'Soft (namespace) vs hard (vCluster / per-cluster)'
  - 'Perimeter · Identity · Tenant zone · Vault'

section_5_real_world:
  count: 4
  examples:
    - 'A 100-engineer fintech runs STRIDE on every Service handling money.'
    - 'A 25-engineer SaaS isolates customers via Namespace + RBAC + NetPol default-deny + PSA Restricted + per-customer ResourceQuota.'
    - 'A health-tech runs PHI for multiple hospitals.'
    - 'A startup put all security at the WAF + Ingress.'

section_6_animated_svg:
  modes_count: 1
  description: 'See preview-kubernetes-adv-sec-lesson-01.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-adv-sec-lesson-01.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Threat-model first; zero-trust everywhere; multi-tenant isolation by design. Four layers: perimeter, identity, tenant zone, vault. Each layer authenticates + authorises independently. A breach at one layer must not become a breach of the next.**'

fact_check_anchors:
  - 'https://kubernetes.io/docs/concepts/kubernetes-security'
  - 'https://kubernetes.io/docs/'
