# Intake brief — A2, K-AKS
# Topic: A2 · Azure Identity and Access (Entra ID, Workload Identity, Azure RBAC)
# Module A2 · Entra and Workload Identity

lesson:
  title: 'A2 · Azure Identity and Access (Entra ID, Workload Identity, Azure RBAC)'
  slug: 'a2-entra-and-workload-identity'
  domain: 'kubernetes'
  course_slug: 'azure-aks'
  position: 02
  granularity: 'module'
  brief_drafted_on: '2026-05-03'
  central_metaphor: 'K-Campus · Registrar''s Office — The **Registrar''s Office** sits in the middle of K-Campus. Everyone — Faculty, students, contractors, robots — must check in at the Registrar before they''re allowed into any building. The Registrar is'
  module: 'Module A2 · Entra and Workload Identity'

learner_profile:
  prerequisites:
    - 'K-COM curriculum complete (L01-L44).'
    - 'Azure basics: Entra ID, VNet, VMSS, LB, Disks/Files, Monitor, Key Vault.'
    - 'K-AKS modules 1-1 (cumulative).'
  assumed_zero_knowledge_of: []

learning_outcomes:
  - 'Learner can explain two identity surfaces — keep them separated.'
  - 'Learner can explain human auth — kubelogin and disable local accounts.'
  - 'Learner can explain pod auth — Workload Identity (federated credentials).'

sections:
  - 'Two identity surfaces — keep them separated'
  - 'Human auth — kubelogin and disable local accounts'
  - 'Pod auth — Workload Identity (federated credentials)'
  - 'ACR + Key Vault — the two adjacent services'

section_5_real_world:
  count: 4
  examples:
    - 'A bank requires every `kubectl` session against prod to require: a compliant Intune device, MFA every 8 hours, only from corporate VPN egress IPs.'
    - 'Pre-existing AKS cluster uses aad-pod-identity for ~30 microservices.'
    - 'An ML training Pod needs read on a 200-TB blob container and three secrets from Key Vault.'
    - 'A SaaS where each tenant''s workload runs in its own namespace with its own ServiceAccount.'

section_6_animated_svg:
  modes_count: 1-3
  description: 'See preview-kubernetes-aks-lesson-02.html Section 6 for the live animation.'

section_7_flashcards_quiz:
  flashcards: 8
  quiz_questions: 3

shared_artifacts:
  preview_page: { file: '/preview-kubernetes-aks-lesson-02.html' }
  lesson_md: { file: 'lesson.md' }
  flashcards: { file: 'flashcards.yaml', count: 8 }
  quiz: { file: 'quiz.yaml', count: 3 }

bar:
  - '**Two surfaces: Entra ID authenticates humans to kube-apiserver (Azure RBAC for K8s preferred); Workload Identity authenticates Pods to Azure services (federated credentials replace deprecated Pod Identity). Disable local accounts; use managed identities, not service principals.**'

fact_check_anchors:
  - 'https://learn.microsoft.com/azure/aks/'
  - 'https://learn.microsoft.com/azure/aks/best-practices'
  - 'https://kubernetes.io/docs/'
